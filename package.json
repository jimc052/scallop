{
  "name": "scallop",
  "version": "1.0.0",
  "description": "Android 開發輔助工具",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron  .",
    "packager": "electron-packager . scallop version=1.0.0 --platform=darwin --arch=x64 --out=./dist/ --asar=false  --icon=icon.icns --overwrite --ignore=output --prune"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^11.2.0",
    "fix-path": "^3.0.0",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "electron-packager": "^15.2.0"
  }
}
